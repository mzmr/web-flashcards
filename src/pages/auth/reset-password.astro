---
import Layout from "@/layouts/Layout.astro";
import { ResetPasswordForm } from "@/components/auth/ResetPasswordForm";

const handleSubmit = async (data: { email: string }) => {
  // Tymczasowa implementacja - będzie zaktualizowana później
  console.log("Reset password attempt:", data);
};
---

<Layout title="Reset hasła | Fiszki">
  <main class="container flex-1 flex items-center justify-center min-h-[calc(100vh-4rem)] w-full">
    <ResetPasswordForm client:load onSubmit={handleSubmit} />
  </main>
</Layout>

<style>
  .container {
    max-width: 100% !important;
    width: 100% !important;
  }
</style>
